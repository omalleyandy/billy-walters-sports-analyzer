[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "walters-analyzer"
version = "0.1.0"
description = "Billy Walters Sports Analyzer (WSA) — CLI + scraping skeleton using uv."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Andy + ChatGPT", email = "noreply@example.com" }]

dependencies = [
  "orjson>=3.11.4",
  "pyarrow>=21.0.0",
  "python-dotenv>=1.2.1",
  "scrapy>=2.13.3",
  "scrapy-playwright>=0.0.44",
  "playwright>=1.47.0",
  "playwright-stealth>=1.0.6",
]

[project.optional-dependencies]
scraping = ["beautifulsoup4>=4.12","tenacity>=8.2"]
dev = ["pytest>=7.4","pytest-cov>=4.1","ruff>=0.6.0","mypy>=1.11"]

[project.scripts]
walters = "walters_analyzer.cli_interface:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"

[tool.ruff]
src = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"

[tool.mypy]
python_version = "3.11"
packages = ["walters_analyzer"]
ignore_missing_imports = true
