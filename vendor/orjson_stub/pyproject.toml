[project]
name = "orjson"
version = "3.11.4"
description = "Pure-Python fallback for orjson (stubbed for offline environments)."
requires-python = ">=3.9"
readme = "README.md"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
